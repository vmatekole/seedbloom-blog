// Generated by CoffeeScript 1.12.0
(function() {
  var GF;

  GF = {};

  this.mobileWidth = 768;

  GF.toggleMobileNav = function() {
    var $header;
    if ($(window).width() > 768) {
      return;
    }
    $header = $('#header');
    return $header.toggleClass('toggle');
  };

  GF.resizeDiv = function() {
    var headerHeight;
    headerHeight = $('#header').height();
    $('#spacer').css('margin-top', headerHeight);
    if ($(window).width() >= mobileWidth) {
      return $('.navbar-collapse').removeClass('animated');
    } else {
      return $('.navbar-collapse').addClass('animated');
    }
  };

  GF.initPage = function() {
    $('[data-toggle=modal]').on('click', function(e) {
      var id;
      e.preventDefault();
      id = $(this).attr('data-target');
      return $(id).modal('show');
    });
    $('body').css('background-image', "");
    $('.nav ul li a').click(function(e) {
      $(this).parent().toggleClass('active');
      $(this).parent().siblings().removeClass('active');
      return e.stopPropagation();
    });
    window.onresize = function() {
      return GF.resizeDiv();
    };
    return GF.resizeDiv();
  };

  GF.selectAnchor = function() {
    var hash;
    hash = window.location.hash;
    if (!($(hash).length > 0)) {
      return;
    }
    return $('html,body').stop().animate({
      scrollTop: $(hash).offset().top - 100
    }, 1500, 'easeInOutExpo');
  };

  $(document).ready(function() {
    var $window;
    GF.initPage();
    $('.navbar-toggle').on('click', function() {
      return GF.toggleMobileNav();
    });
    $window = $(window);
    return $window.scroll(function() {
      var collapseNav;
      collapseNav = function() {
        return $('#header').removeClass('toggle');
      };
      window.setTimeout(collapseNav, 250);
      if ($window.scrollTop() > 0) {
        return $('#header').addClass('sticky');
      } else {
        return $('#header').removeClass('sticky');
      }
    });
  });

}).call(this);

//# sourceMappingURL=index.js.map
